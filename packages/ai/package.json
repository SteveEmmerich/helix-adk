{
  "name": "@helix/ai",
  "version": "0.1.0",
  "description": "Unified LLM provider API with streaming, tool calling, and cost tracking",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --sourcemap",
    "dev": "bun build --watch src/index.ts --outdir dist --target bun --format esm",
    "test": "bun test",
    "check": "bunx @biomejs/biome check src"
  },
  "dependencies": {
    "eventsource-parser": "^3.0.0"
  }
}
