{
  "name": "@helix/evals",
  "version": "0.1.0",
  "description": "Evaluation framework for testing and benchmarking helix agents",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --sourcemap",
    "dev": "bun build --watch src/index.ts --outdir dist --target bun --format esm",
    "test": "bun test",
    "check": "bunx @biomejs/biome check src"
  },
  "dependencies": {
    "@helix/ai": "*",
    "@helix/core": "*"
  }
}