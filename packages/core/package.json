{
  "name": "@helix/core",
  "version": "0.1.0",
  "description": "Agent loop, session management, tool execution, and middleware pipeline",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --sourcemap",
    "dev": "bun build --watch src/index.ts --outdir dist --target bun --format esm",
    "test": "bun test",
    "check": "bunx @biomejs/biome check src"
  },
  "dependencies": {
    "@helix/ai": "*",
    "@helix/memory": "*",
    "@helix/security": "*"
  }
}
